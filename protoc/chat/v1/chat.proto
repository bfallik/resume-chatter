syntax = "proto3";
package chat.v1;
// option go_package = "./proto";

message AskRequest {
    string document_path = 1;
    string question = 2;
}

message AskResponse {
    string response = 1;
}

service ChatService {
    rpc Ask(AskRequest) returns (AskResponse);
}